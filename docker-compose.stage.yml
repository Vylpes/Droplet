version: "3"

volumes:
  stage_database_data:

services:
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: droplet
    ports:
     - "3011:27017"

  mail:
    image: namshi/smtp
    ports:
      - "3013:25"