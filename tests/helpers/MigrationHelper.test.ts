describe("Up", () => {
    test.todo("EXPECT queryRunner.query to be called for each query file");
});

describe("Down", () => {
    test.todo("EXPECT queryRunner.query to be called for each query file");
});