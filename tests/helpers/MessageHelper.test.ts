describe("constructor", () => {
    test.todo("EXPECT properties to be set");
});

describe("Info", () => {
    test.todo("EXPECT flash to be set with info");

    test.todo("GIVEN session.save returns error, EXPECT reject to be called");
});

describe("Error", () => {
    test.todo("EXPECT flash to be set with error");

    test.todo("GIVEN session.save returns error, EXPECT reject to be called");
});