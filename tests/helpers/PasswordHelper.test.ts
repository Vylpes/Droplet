describe("GenerateRandomHashedPassword", () => {
    test.todo("EXPECT to return a hashed password");
});

describe("GenerateRandomToken", () => {
    test.todo("EXPECT to return a random token");
});

describe("RandomString", () => {
    test.todo("EXPECT to return a random string of given length");
});