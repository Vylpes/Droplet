describe("OnPost", () => {
    test.todo("EXPECT validator to be defined");

    test.todo("EXPECT router to be defined");

    test.todo("EXPECT order to be created");

    test.todo("GIVEN postagePolicyId is INHERIT, EXPECT order to inherit postage policy from listing");

    test.todo("GIVEN postagePolicy is null, EXPECT postage policy to not be added");
});